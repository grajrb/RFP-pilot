## Packages
framer-motion | Complex animations and page transitions
clsx | Conditional class names utility
tailwind-merge | Utility for merging tailwind classes (essential for reusable components)
react-hook-form | Form state management
@hookform/resolvers | Zod integration for react-hook-form
date-fns | Date formatting

## Notes
- Tailwind config must extend font family to support 'Outfit' (display) and 'Inter' (body).
- The 'Simulate Email' feature interacts with /api/webhooks/email to mimic external services.
- AI Generation endpoint returns structured JSON which should be prettified in the UI.
